import React, { FC } from 'react';
import { ScrollView, Text } from 'react-native';

export const TodoSettingsScreen: FC = () => {
  return (
    <ScrollView>
      <Text>todoSettings</Text>
    </ScrollView>
  );
};
